version: '3'
services:
    backend:
        ports:
            - '8000:80'
        volumes:
            - ./backend/default.conf:/etc/nginx/conf.d/default.conf:ro
            - ./api:/var/www/html:ro
    api:
        user: ${UID}:${GID}
        volumes:
            - ./api:/var/www/html:rw
